Hello, VIRTEL
print "Hello, world!";

Data Types
  nil: null value
  boolean: true, false
  number: floating-point
  string: "text"

Comments
  // single-line comment

Variables
  var name = "Lox";

Unary Operators
  -a → negation
  !a → logical NOT

Binary Operators
  a * b → multiplication
  a / b → division
  a + b → addition or string concatenation
  a - b → subtraction
  a > b → greater than
  a >= b → greater than or equal
  a < b → less than
  a <= b → less than or equal
  a == b → equality
  a != b → inequality

Control Flow
  if (condition) {
    // code
  } else if (condition) {
    // code
  } else {
    // code
  }

  while (condition) {
    // code
  }

  for (var i = 0; i < 10; i = i + 1) {
    // code
  }

Functions
  fun greet(name) {
    print "Hello, " + name;
  }

  greet("World");

Classes
  class Person {
    init(name) {
      this.name = name;
    }

    greet() {
      print "Hi, I'm " + this.name;
    }
  }

  var alice = Person("Alice");
  alice.greet();

Inheritance
  class Student < Person {
    study() {
      print this.name + " is studying.";
    }
  }

Standard Library
  clock() → returns current time in seconds since program start
